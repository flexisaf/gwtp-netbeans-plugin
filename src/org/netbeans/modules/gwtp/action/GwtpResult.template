<#assign licenseFirst = "/*">
<#assign licensePrefix = " * ">
<#assign licenseLast = " */">
<#include "${project.licensePath}">

/**
 * Date Created: ${date}
 *
 */

<#if package?? && package != "">
package ${package};
</#if>

import com.gwtplatform.dispatch.rpc.shared.Result;

/**
 *
 * @author ${user}
 */
public class ${targetName}Result implements Result {

    public TalkToServerResult() {
    }

}